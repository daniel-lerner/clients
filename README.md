# clients
This library was created to summarize all the functions used to clean and merge clients based on CPF, name and address columns in a dataframe. It also counts the number of occurrences of each value in a column, performs the fuzzy between two columns row by row and mark duplicated clients.
